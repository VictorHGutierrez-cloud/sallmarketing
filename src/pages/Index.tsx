const Index = () => {
  return (
    <main
      style={{
        minHeight: "100vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontFamily: "system-ui, sans-serif",
        background: "#111",
        color: "#fff",
        margin: 0,
      }}
    >
      <p style={{ fontSize: "1.25rem", opacity: 0.85 }}>This page is not available.</p>
    </main>
  );
};

export default Index;
